# ダッシュボード v2.0 改修内容

## 概要

営業部ダッシュボードの大幅改修を実施し、**スコアリングシステム**と**業務区分マスター管理**を導入しました。
これにより、管理者は組織全体のパフォーマンスと労務リスクをより詳細に把握・分析できるようになります。

---

## 主要な新機能

### 1. 全体サマリータブ（新設）

ダッシュボードのデフォルトビューとして「全体サマリー」タブを新設しました。

#### 表示内容

- **KPIカード**
  - 平均貢献スコア（0-100）
  - 平均負荷リスク（数値）
  - 要対応者数（Lv.3以上）

- **バブルチャート**
  - X軸: 貢献スコア（0-100）
  - Y軸: 負荷リスク（数値）
  - 各従業員を気泡で表示
  - リスクレベル（Lv.3以上）は赤色で強調表示

- **リスクレベル内訳（円グラフ）**
  - Lv.1（低）: 緑
  - Lv.2（中）: 黄
  - Lv.3（高）: オレンジ
  - Lv.4（極めて高）: 赤

- **負荷リスク・ヒートマップ**
  - 日別の組織全体の平均リスクをカレンダー形式で表示
  - 色の濃さでリスク度合いを可視化

---

### 2. 業務区分マスター管理

設定画面に「業務区分マスター設定」セクションを追加しました。

#### 機能

- **業務区分の追加・編集・削除**
  - テーブル形式で業務区分を管理
  - 業務区分名、重要度（0-100）、表示色を自由に設定

- **重要度の合計チェック**
  - 全業務の重要度合計が100になるよう自動チェック
  - 合計が100でない場合は警告表示
  - 合計が100の場合は緑色で確認表示

- **表示色のカスタマイズ**
  - カラーピッカーで各業務の表示色を自由に設定
  - 設定した色がすべてのグラフに即座に反映

#### 保存方法

- 「業務区分を保存」ボタンで LocalStorage に保存
- 保存後、すべてのグラフが自動的に更新

---

### 3. スコアリングシステム

#### 3.1 貢献スコア（Contribution Score）

**計算式:**
```
貢献スコア = Σ(業務区分ごとの作業時間割合 × その業務の重要度)
```

**特徴:**
- 0-100の範囲で算出
- 業務区分ごとの重要度設定に基づく
- 実作業時間の配分を反映

**例:**
- 重要度が高い業務に多くの時間を費やす → スコアが高い
- 重要度が低い業務ばかり → スコアが低い

#### 3.2 負荷リスクスコア（Risk Score）

**計算式:**
```
リスクスコア = (時間外労働時間 × ot重み) 
             + (深夜労働時間 × late重み) 
             + (休憩不足日数 × break重み) 
             + (休暇中業務時間 × vacation重み)
```

**重み付けの設定:**
- 時間外労働: 1.0pt/時間（デフォルト）
- 深夜労働（22:00-05:00）: 1.5pt/時間（デフォルト）
- 休憩不足: 2.0pt/回（デフォルト）
- 休暇中業務: 1.2pt/時間（デフォルト）

**設定方法:**
- 設定画面の「負荷スコア重み付け設定」で各重みを調整可能
- 「重み付けを保存」ボタンで保存

#### 3.3 リスクレベル判定

スコアに基づいて、以下の4段階に自動分類:

| リスクレベル | スコア範囲 | 説明 | 色 |
|-------------|----------|------|-----|
| Lv.1 | 0-4 | 低リスク | 緑 |
| Lv.2 | 5-9 | 中程度 | 黄 |
| Lv.3 | 10-14 | 高リスク | オレンジ |
| Lv.4 | 15+ | 極めて高 | 赤 |

---

### 4. 設定画面の拡張

#### 新規セクション

1. **業務区分マスター設定**（全幅）
   - 業務区分の追加・編集・削除
   - 重要度の合計チェック

2. **負荷スコア重み付け設定**
   - 時間外労働の重み
   - 深夜労働の重み
   - 休憩不足の重み
   - 休暇中業務の重み

3. **アラート閾値設定**（従来）
   - 時間外アラート
   - 休暇中業務アラート
   - 休憩不足アラート

4. **パスワード変更**（従来）

---

## UI・グラフの改善

### グラフの動的化

すべての業務区分グラフが、マスター設定に基づいて動的に色付けされるように修正:

- 業務区分別比率（ドーナツグラフ）
- 個人別業務時間（積み上げ棒グラフ）
- 部門別業務区分比率（積み上げ棒グラフ）
- 個人別業務区分内訳（ドーナツグラフ）

### ナビゲーション

- 「全体サマリー」タブをデフォルト表示に変更
- タブ順序: サマリー → ダッシュボード → 部門別 → 個人別 → 日別 → アラート → CSV取込 → 設定

---

## データ永続化

### LocalStorage キー

- `dash_settings`: 設定情報（業務区分マスター、重み付け、アラート閾値）
- `dash_records`: CSV取込データ

### 設定の初期化

初回起動時は以下のデフォルト値が使用されます:

**業務区分マスター（デフォルト）:**
- 九電碍・点: 20%
- 九電管路: 15%
- 他電力碍・点: 15%
- 直送商: 10%
- 在庫商: 10%
- 外販製品（非電力）: 10%
- TKD: 10%
- 社内対応: 10%

**重み付け（デフォルト）:**
- 時間外: 1.0pt/時間
- 深夜: 1.5pt/時間
- 休憩不足: 2.0pt/回
- 休暇中業務: 1.2pt/時間

---

## 技術仕様

### 新規関数

| 関数名 | 説明 |
|-------|------|
| `calculateScores(empId)` | 従業員のスコアを計算 |
| `renderSummaryTab()` | 全体サマリータブを描画 |
| `renderSummaryBubbleChart(scores)` | バブルチャートを描画 |
| `renderRiskLevelPieChart(scores)` | リスクレベル円グラフを描画 |
| `renderSummaryHeatmap()` | リスク・ヒートマップを描画 |
| `renderWorkTypeMasterTable(master)` | 業務区分マスターテーブルを描画 |
| `addWorkTypeRow()` | 業務区分行を追加 |
| `deleteWorkTypeRow(btn)` | 業務区分行を削除 |
| `updateWorkTypeTotal()` | 重要度合計を更新 |
| `saveWorkTypeMaster()` | 業務区分マスターを保存 |
| `saveRiskWeights()` | 重み付けを保存 |
| `getWorkColor(name)` | 業務区分の表示色を取得 |

### 修正関数

- `loadSettings()`: 業務区分マスターと重み付けの読み込みに対応
- `loadSettingsForm()`: 新規セクションのフォーム初期化に対応
- `renderAll()`: 全体サマリータブの描画を追加
- `switchTab()`: サマリータブの切り替えに対応

---

## 互換性

- 既存の CSV 形式: 変更なし
- 既存のアラート機能: 継続利用可能
- 既存のグラフ: 動的化により、業務区分マスター設定に自動対応

---

## 今後の拡張予定

1. 部門別（本社/福岡支社）のスコアリング分析
2. 月別・年別のスコアリング推移グラフ
3. 従業員個別のスコアリング詳細画面
4. リスク改善提案の自動生成

---

## バージョン情報

- **バージョン**: v2.0
- **リリース日**: 2026年5月31日
- **対応ブラウザ**: Chrome, Firefox, Safari, Edge（最新版推奨）
