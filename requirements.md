# 業務時間ダッシュボード 改修要件定義（Manus変更指示書より）

## 1. データ保存構造の刷新
- **月別履歴保存**: CSV取込時、既存データを上書きせず、月別の履歴として保持する。
- **重複防止ルール**: 同月のデータを複数回取り込んだ場合は、後から取り込んだデータで上書きする（確認メッセージを表示）。
- **保存容量への配慮**: localStorageの制限を考慮し、IndexedDBの利用も検討。保存済み月数・件数を表示し、古いデータの削除機能を提供。

## 2. 集約期間設定機能
- **UI追加**: ダッシュボード上部に「集約開始月」と「集約終了月」の選択（YYYY年MM月形式）を追加。
- **集計対象**: 
    - 単月表示：開始月＝終了月の場合。
    - 複数月表示：開始月〜終了月の全データを集計対象とする。
- **反映範囲**: ダッシュボード、部門別分析、個人別分析、日別分析、アラート、CSV取込画面のデータ確認欄。

## 3. 各画面の表示仕様
- **ダッシュボード**: 複数月選択時は「2026年05月〜2026年06月 データ」のように表示。
- **部門別分析**: 期間合計と期間平均を表示。1人・1営業日あたりの指標を主表示。
- **個人別分析**: 期間合計、月平均、1営業日あたり、月別推移を表示。
- **日別分析**: 期間全体の推移グラフと異常日一覧を優先。
- **アラート画面**: 選択期間内のアラートを月別にグルーピングして表示。

## 4. 集計・計算ロジック
- **前月比/前期間比**: 
    - 単月：前月と比較。
    - 複数月：同じ長さの直前期間と比較（例：5-7月なら2-4月と比較）。
- **営業日数補正**: 複数月の場合、各月の営業日数を合算して補正を行う。
    - 補正後時間 = 期間合計時間 ÷ 対象人数 ÷ 期間営業日数合計

## 5. CSV取込画面の拡張
- **保存データ一覧**: 対象月、件数、人数、取込日時を表示し、削除・再取込操作を可能にする。
- **データ管理**: 全データ削除、保存データのエクスポート（JSON/CSV）、インポート機能。

## 6. その他
- **README.mdの更新**: 上記仕様、ルール、操作方法を追記。
- **最終的な運用イメージ**: 管理者が各担当者から受け取った月次CSVを取り込み、任意期間を選択して分析する。
